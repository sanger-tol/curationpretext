nextflow_process {

    name "Test Process TELOMERE_WINDOWS"
    script "../main.nf"
    process "TELOMERE_WINDOWS"

    tag "modules"
    tag "modules_sangertol"
    tag "telomere"
    tag "windows"
    tag "telomere/windows"

    config "./nextflow.config"

    test("idFanCani4 - telomere") {

        when {
            params {
                telomere_window_cut = 99.9
            }

            process {
                """
                input[0] = [
                    [ id: "idFanCani4" ],
                    file(params.modules_testdata_base_path + 'resources/modules/telomere/idFanCani4/idFanCani4.telomere', checkIfExists: true)
                ]
                """
            }
        }

        then {
            assertAll(
                { assert process.success               },
                { assert snapshot(process.out).match() }
            )
        }
    }

    test("idFanCani4 - telomere - stub") {

        options "-stub"

        when {
            process {
                """
                input[0] = [
                    [ id: "idFanCani4" ],
                    file(params.modules_testdata_base_path + 'resources/modules/telomere/idFanCani4/idFanCani4.telomere', checkIfExists: true)
                ]
                """
            }
        }

        then {
            assertAll(
                { assert process.success               },
                { assert snapshot(process.out).match() }
            )
        }
    }
}
